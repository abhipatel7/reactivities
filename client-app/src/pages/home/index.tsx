import React, { FC } from 'react';
import { Container } from 'semantic-ui-react';

const Home: FC = () => {
  return <Container style={{ marginTop: '7em' }}>Home</Container>;
};

export default Home;
